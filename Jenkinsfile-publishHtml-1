pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Test') {
      steps {
        withSonarQubeEnv(installationName: 'sq1') {
       
        #sh './mvnw clean org.sonarsource.scanner.maven:sonar-maven-plugin:10.3.0.82913:sonar'
        
        }
      }
    }
  }
}
